import type { BgmiLiteRedeemCodeItem } from "@/src/lib/bgmiLiteRedeemCodes";

/** No built-in archive samples — admin starts empty. */
export const DEFAULT_BGMI_LITE_REDEEM_EXPIRED_CODES: BgmiLiteRedeemCodeItem[] = [];
