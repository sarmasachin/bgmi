/** Built-in live samples — kept empty so admin starts clean; use Reset only if you want seed archive. */
import type { FreeFireRedeemCodeItem } from "@/src/lib/freeFireRedeemCodes";

export const DEFAULT_FREE_FIRE_REDEEM_CODES: FreeFireRedeemCodeItem[] = [];
